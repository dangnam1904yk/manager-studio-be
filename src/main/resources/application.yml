spring:
    application:
        name: managerstudio
    datasource:
        url: jdbc:mysql://localhost:3306/manager_studio?useSSL=false&serverTimezone=Asia/Ho_Chi_Minh&allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=UTF-8
        username: root
        password: dangnam
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
        properties:
            hibernate:
                format_sql: true
                generate_statistics: true
server:
    port: 8080

security:
  jwt:
    secret-key:
    expiration: 3600

